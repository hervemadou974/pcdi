{% extends 'base.html.twig' %}

{% block title %}Ordinateur{% endblock %}

{% block body %}
    <h1>Ordinateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordinateur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ordinateur.name }}</td>
            </tr>
            <tr>
                <th>IsAvailable</th>
                <td>{{ ordinateur.isAvailable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ordinateur_index') }}">back to list</a>

    <a href="{{ path('app_ordinateur_edit', {'id': ordinateur.id}) }}">edit</a>

    {{ include('ordinateur/_delete_form.html.twig') }}
{% endblock %}
